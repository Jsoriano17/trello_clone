
<%= form_with(model: @board, local: true, class: "form-group") do |f| %>

    <%= f.label :title%>
    <%= f.text_field :title %>
    <br/>
    <%= f.submit nil, class:"btn btn-primary"%>
    
<% end %>
